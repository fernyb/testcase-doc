describe("Authentication", () => {
  context("Login", () => {
    it("user can log in", () => {
      // tc: click username field
      // tc: enter username
      // tc: press tab button
      // tc: click password field
      // tc: enter user password
      // tc: click submit button
      // tc: page should redirect to user profile
      // tc: verify page is user profile
    })
  });
});